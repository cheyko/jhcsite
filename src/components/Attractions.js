import React from "react";
import withContext from "../withContext";

const Attractions = props => {

    return (
        <div className="hero">
            <div className="hero-body">
                <h1>Attractions</h1>
            </div>
        </div>
    )
}
export default withContext(Attractions);